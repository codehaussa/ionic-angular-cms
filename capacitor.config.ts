import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codehaussa.cms',
  appName: 'CodeHaus CMS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
